# source bashrc so that "module" is available on alpine and blanca sessions
source /etc/bashrc

# Export the module function if it exists
[[ $(type -t module) == "function" ]] && export -f module

# Find available port to run server on
port=$(find_port)
